{
  "buildCommand": "npm run sdk:build && mkdir -p public && cp sdk/web/dist/geolock.umd.js public/geolock.umd.js && prisma generate && prisma migrate deploy && next build",
  "framework": "nextjs"
}
